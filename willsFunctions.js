const add = (num1, num2) => {
    return num1 + num2
}

const multiply = (num1, num2) => {
    return num1 * num2
}


const name = "willsFunctions.js"

module.exports = {
    add,
    multiply,
    name,
    desc: "These functions shall help you.",
    noOfFunctions: 2,
}